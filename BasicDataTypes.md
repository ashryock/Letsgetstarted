  // Data type is String
  "Mark is my name"

  // Data type is Integer
  Int --> 1

  // Data type is Float
  Float --> 1.1

  // Data type is Array (a list of things)
  bob = [1,2,3,4,5, "Mark"]

  // Data type is boolean -- true or false
  Boolean True | False

  // methods
  bob.length
        ^^^
        This is a "method" -- a way of doing things to something in programming

  // This is a variable -- a container for storing any sort of data type
  var locationOfChildrensHungerFund = "ADDRESS WILL GO HERE"

  function getSomething(parameter1, parameter2) {  ...    }
  ^^^         ^^^             ^^^                   ^^^
   |           |               |                     |
  key word |  name of func.   parameter/arugment     Function body
  that declares
  a function
